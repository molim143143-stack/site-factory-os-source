工程工单（分4阶段）**，每一阶段都有：

* 🎯 目标
* 📦 必须实现的模块
* 🚫 禁止实现（防止越界）
* 📂 必须产出文件
* ✅ 验收标准（必须跑通）
* ⚠️ 强约束（防止写崩）

---

# 🧠 总前提（必须放在工单最顶部）

```text
你正在实现 Site Factory OS（SFS）系统。

必须严格遵守：

1. 所有操作必须走 Task Engine
2. 所有写入必须带 request_id（幂等）
3. 所有任务必须带 trace_id
4. 禁止绕过 ValidateNode
5. 禁止直接操作 GitHub / DNS（必须通过 Engine）
6. 所有错误必须使用 error_code_dictionary.md
7. 所有结构必须遵守 schema
8. 所有模块必须可独立运行和测试
9. 每一阶段必须能运行，不允许“只写结构不实现”
```

---

# 🚀 阶段一：核心骨架 + 建站链路（MVP）

---

## 🎯 目标

> **实现：从 0 → 创建网站 → GitHub → Cloudflare → 可访问页面**

---

## 📦 必须实现模块

```text
core/
- engine_boot.py
- workflow_router.py
- node_executor.py
- f1_build_engine.py
- f3_site_manager.py
- f3.1_alias_engine.py
- f6_deploy_engine.py
- f7_dns_engine.py
- f8_task_engine.py（必须含幂等）
- error_engine.py
- audit_engine.py
```

```text
api/
- site_api.py
- domain_api.py
- deploy_api.py
- task_api.py
```

```text
integrations/
- github.py
- cloudflare.py
- namecom.py（仅NS提示，不做自动购买）
```

```text
storage/
- registry.json
- aliases.json
- tasks.json
- domains.json
```

---

## 🚫 禁止实现

```text
❌ CMS
❌ Bulk Import
❌ I18n完整系统
❌ DIY Builder
❌ SEO
❌ Payment
```

---

## ⚠️ 强约束（关键）

```text
1. Task Engine 必须实现 request_id 幂等
2. 同一 site_id 禁止并发任务
3. DNS只做：
   - Cloudflare zone 创建
   - 返回 NS
   - 检测 NS
4. Name.com 只做提示，不做 API 调用
```

---

## 📂 必须实现流程（核心）

```text
POST /site/create
↓
Task Engine
↓
Build Engine
↓
GitHub repo 创建
↓
写入 CNAME
↓
Cloudflare 创建 zone
↓
返回 NS
↓
任务进入 waiting_ns 状态
↓
手动触发 DNS check
↓
通过后 deploy
```

---

## ✅ 验收标准

```text
✔ 能创建 site_id
✔ 能生成 alias
✔ 能创建 GitHub repo
✔ 能写入 CNAME
✔ 能创建 Cloudflare zone
✔ 能返回 NS
✔ 能检测 NS
✔ 能 deploy GitHub Pages
✔ 网站可访问
✔ 所有步骤都有 task log
```

---

# 🚀 阶段二：CMS + 商品 + 支付链接

---

## 🎯 目标

> **实现：可以往网站里加内容和商品，并发布上线**

---

## 📦 必须实现

```text
core/
- f2_cms_engine.py
- f11_payment_engine.py
```

```text
api/
- cms_api.py
- product_api.py
- payment_api.py
```

```text
storage/
- cms.json
- products.json
- payments.json
```

---

## ⚠️ 强约束

```text
1. 所有发布必须走 Task Engine
2. 不允许直接写 GitHub
3. 必须支持 draft / published
4. product.price 必须校验为数字
```

---

## 📂 必须支持

```text
POST /cms/article/create
POST /cms/product/create
POST /cms/product/publish
```

---

## ✅ 验收

```text
✔ 可以创建文章
✔ 可以创建商品
✔ 可以绑定支付链接
✔ 可以发布并生成页面
✔ 发布触发 deploy
✔ 页面可访问
```

---

# 🚀 阶段三：Bulk Import（核心赚钱模块）

---

## 🎯 目标

> **实现：文件夹 → 多站内容 → 自动导入 + 发布**

---

## 📦 必须实现

```text
core/
- f4_bulk_engine.py
```

```text
api/
- bulk_api.py
```

---

## ⚠️ 强约束（必须执行你刚定义的规则）

```text
1. Bulk 必须三阶段：

   scan
   validate
   execute

2. 不允许边执行边报错

3. 错误必须使用：

   BULK_***

4. 必须实现字段校验：

   price → 数字
   images → 必须存在
   language → 必须在 enabled_languages

5. 必须支持：

   preview
   report
   retry
```

---

## 📂 必须支持目录结构

```text
bulk_upload/
  site_001/
    config.json
    images/
    i18n/en/product.txt
```

---

## ✅ 验收

```text
✔ 能识别 site_id
✔ 能校验 config.json
✔ 能校验 product/article
✔ 能校验图片存在
✔ 错误能定位到文件+行
✔ validate 不执行写入
✔ execute 才写入
✔ 成功后自动 deploy
✔ 失败能生成 report
✔ 失败能 retry
```

---

# 🚀 阶段四：任务系统强化 + 回滚 + 稳定性

---

## 🎯 目标

> **让系统“不会炸”**

---

## 📦 必须实现

```text
core/
- 完整 f8_task_engine.py
- rollback_strategy.py
- error_engine.py（升级版）
```

---

## ⚠️ 强约束

```text
1. 必须实现状态机：

pending → running → success
                ↘ failed → retrying

2. 必须实现：

request_id 幂等
site_id 任务锁

3. 回滚规则必须实现：

GitHub → 可回滚
Bulk → 不自动删除成功项
DNS → 不允许自动回滚
```

---

## 📂 必须支持

```text
POST /task/retry/{task_id}
POST /task/cancel/{task_id}
POST /deploy/rollback/{deploy_id}
```

---

## ✅ 验收

```text
✔ 同 request_id 不重复创建任务
✔ 同站点不能并发任务
✔ 失败任务可 retry
✔ deploy 可回滚
✔ 所有任务有完整日志
✔ 错误统一 error_code
```

---
你正在实现 Site Factory OS（SFS）系统的阶段一。

【目标】
实现从创建网站到部署上线的完整链路（使用 mock 的 GitHub/Cloudflare 也可以），必须可运行、可测试。

【必须实现的模块】

* FastAPI 网关（main.py）
* Task Engine（幂等 request_id + 状态机 + 任务锁）
* Site Manager（site_id + alias）
* Build Engine（创建站点流程）
* Deploy Engine（GitHub mock）
* DNS Engine（Cloudflare mock + NS返回 + DNS检测）
* Alias Engine
* Error Engine（统一 error_code）
* Storage（JSON文件）

【API必须实现】
POST /site/create
GET  /site/list
GET  /site/{site_id}

POST /domain/ns/{domain}
GET  /domain/status/{domain}

GET  /task/{task_id}

【核心流程（必须严格实现）】

1. /site/create 接收：

   * domain
   * alias
   * request_id

2. Task Engine：

   * 校验 request_id 幂等
   * 创建 task
   * 状态 pending → running

3. Build Engine：

   * 生成 site_id
   * 保存 registry
   * 创建 mock GitHub repo
   * 写入 CNAME（仅记录）

4. DNS Engine：

   * 创建 mock Cloudflare zone
   * 返回 NS（固定模拟）
   * task 状态进入 waiting_ns

5. DNS 检测：

   * 提供接口手动触发
   * 模拟 NS 生效

6. Deploy：

   * mock deploy 成功
   * task → success

【强制约束】

1. 所有操作必须走 Task Engine
2. 必须实现 request_id 幂等（重复请求返回同一 task）
3. 同一个 site_id 不允许并发任务
4. 所有错误必须使用 error_code
5. 所有数据必须落 storage/*.json
6. 禁止直接在 API 层写业务逻辑（必须走 engine）

【错误码（必须使用）】
SYSTEM_INVALID_INPUT
SITE_ALREADY_EXISTS
TASK_DUPLICATE_REQUEST_ID
TASK_ALREADY_RUNNING
DNS_NS_NOT_PROPAGATED
DEPLOY_FAILED

【项目结构必须输出】
site-factory-os/
main.py
config.py
core/
api/
integrations/
storage/
logs/

【必须输出】

1. 完整项目代码（可运行）
2. requirements.txt
3. 启动方式说明
4. curl 测试示例

【禁止】

* 禁止实现 CMS / Bulk / I18n / Payment
* 禁止写未实现的“空函数”
* 禁止只给结构不实现逻辑

最终代码必须能：
✔ 启动服务
✔ 创建 site
✔ 返回 task_id
✔ 模拟 DNS
✔ 完成 deploy
✔ 返回 success
二、代码生成模板（给 Agent 起手用）

你可以把下面这段一起丢给 Agent，让它在这个骨架上补全，成功率更高。

📁 main.py（入口）
from fastapi import FastAPI
from api.site_api import router as site_router
from api.domain_api import router as domain_router
from api.task_api import router as task_router

app = FastAPI(title="Site Factory OS")

app.include_router(site_router)
app.include_router(domain_router)
app.include_router(task_router)

@app.get("/")
def root():
    return {"status": "running"}
📁 config.py
DATA_DIR = "storage"
LOG_DIR = "logs"
📁 core/task_engine.py
import uuid
import json
from pathlib import Path

TASK_FILE = Path("storage/tasks.json")

class TaskEngine:

    def __init__(self):
        self.tasks = self._load()

    def _load(self):
        if TASK_FILE.exists():
            return json.loads(TASK_FILE.read_text())
        return {}

    def _save(self):
        TASK_FILE.write_text(json.dumps(self.tasks, indent=2))

    def create_task(self, request_id, task_type, payload):
        # 幂等
        for t in self.tasks.values():
            if t["request_id"] == request_id:
                return t

        task_id = str(uuid.uuid4())
        task = {
            "task_id": task_id,
            "request_id": request_id,
            "type": task_type,
            "status": "pending",
            "payload": payload,
            "logs": []
        }
        self.tasks[task_id] = task
        self._save()
        return task

    def update(self, task_id, status):
        self.tasks[task_id]["status"] = status
        self._save()
📁 core/site_manager.py
import json
from pathlib import Path

SITE_FILE = Path("storage/registry.json")

class SiteManager:

    def __init__(self):
        self.sites = self._load()

    def _load(self):
        if SITE_FILE.exists():
            return json.loads(SITE_FILE.read_text())
        return {}

    def _save(self):
        SITE_FILE.write_text(json.dumps(self.sites, indent=2))

    def create(self, site_id, domain, alias):
        self.sites[site_id] = {
            "site_id": site_id,
            "domain": domain,
            "alias": alias,
            "status": "active"
        }
        self._save()
📁 core/build_engine.py
import uuid

class BuildEngine:

    def create_site(self, domain, alias):
        site_id = f"site_{uuid.uuid4().hex[:6]}"
        return site_id
📁 core/dns_engine.py
class DNSEngine:

    def create_zone(self, domain):
        return {
            "ns": [
                "ns1.cloudflare.com",
                "ns2.cloudflare.com"
            ]
        }

    def check_ns(self, domain):
        # mock always true
        return True
📁 api/site_api.py
from fastapi import APIRouter
from core.task_engine import TaskEngine
from core.build_engine import BuildEngine
from core.site_manager import SiteManager

router = APIRouter()
task_engine = TaskEngine()
builder = BuildEngine()
site_mgr = SiteManager()

@router.post("/site/create")
def create_site(data: dict):
    request_id = data["request_id"]
    domain = data["domain"]
    alias = data["alias"]

    task = task_engine.create_task(request_id, "build_site", data)

    task_engine.update(task["task_id"], "running")

    site_id = builder.create_site(domain, alias)
    site_mgr.create(site_id, domain, alias)

    task_engine.update(task["task_id"], "success")

    return {"task_id": task["task_id"], "site_id": site_id}
📁 requirements.txt
fastapi
uvicorn
▶️ 启动方式
pip install -r requirements.txt
uvicorn main:app --reload
🧪 测试
curl -X POST http://127.0.0.1:8000/site/create \
-H "Content-Type: application/json" \
-d '{
  "request_id": "test_001",
  "domain": "a.com",
  "alias": "测试站"
}'

你正在继续实现 Site Factory OS（SFS）系统，当前是【阶段二】。

当前系统已经具备：

* Site 创建
* Task Engine（幂等）
* GitHub / Cloudflare mock
* 基础 deploy 流程

【本阶段目标】

实现：

1. CMS内容系统（文章）
2. 商品系统（Product）
3. 支付链接系统（Payment Link）
4. 内容发布 → 触发 deploy
5. Telegram / Web API 可调用

⚠️ 本阶段重点：
不是 UI，而是“内容 → 发布 → 部署链路”

---

# 📦 必须实现模块

core/

* f2_cms_engine.py
* f11_payment_engine.py
* publish_engine.py（新增）

api/

* cms_api.py
* product_api.py
* payment_api.py

storage/

* cms.json
* products.json
* payments.json

---

# 🧱 数据模型（必须严格使用）

## Article

{
"article_id": "string",
"site_id": "string",
"title": "string",
"content": "string",
"status": "draft | published"
}

## Product

{
"product_id": "string",
"site_id": "string",
"name": "string",
"price": "float",
"images": ["string"],
"status": "draft | active"
}

## Payment

{
"payment_id": "string",
"site_id": "string",
"provider": "link",
"url": "string"
}

---

# 🌐 API（必须实现）

## CMS

POST /cms/article/create
POST /cms/article/publish
GET  /cms/article/list/{site_id}

## Product

POST /product/create
POST /product/publish
GET  /product/list/{site_id}

## Payment

POST /payment/create
GET  /payment/list/{site_id}

---

# ⚙️ 核心流程（必须实现）

## 文章发布流程

POST /cms/article/publish

↓
Task Engine
↓
ValidateNode
↓
PublishEngine
↓
写入 mock GitHub 内容
↓
DeployEngine
↓
Task → success

---

## 商品发布流程

POST /product/publish

↓
Task Engine
↓
校验 price（必须为数字）
↓
写入商品数据
↓
触发 deploy
↓
Task → success

---

# ⚠️ 强约束（必须执行）

1. 所有 publish 操作必须走 Task Engine

2. 不允许直接写 GitHub（必须通过 DeployEngine）

3. product.price 必须为 float，否则：

   CMS_PRODUCT_PRICE_INVALID

4. Article / Product 必须支持：

   draft → published

5. 发布必须触发 deploy

6. 同一个 site_id：
   不允许并发 publish 任务

---

# 🚨 错误码（必须使用）

CMS_ARTICLE_NOT_FOUND
CMS_ARTICLE_INVALID
CMS_PRODUCT_NOT_FOUND
CMS_PRODUCT_PRICE_INVALID
PAYMENT_LINK_INVALID
TASK_ALREADY_RUNNING

---

# 📂 必须实现存储

cms.json
products.json
payments.json

所有数据必须写入 storage/

---

# 🔁 Task规则（延续阶段一）

必须：

* 支持 request_id 幂等
* 同一 request_id 不重复执行
* Task 状态必须：

  pending → running → success / failed

---

# 🧪 验收标准（必须全部通过）

✔ 能创建 article（draft）
✔ 能 publish article
✔ 页面内容能更新（mock）
✔ 能创建 product
✔ 能 publish product
✔ price 非数字时报错
✔ 能创建 payment link
✔ publish 自动触发 deploy
✔ 所有流程有 task log
✔ API 可用（curl可测试）

---

# 🚫 禁止实现（非常重要）

❌ Bulk Import
❌ I18n多语言
❌ SEO
❌ DIY Builder
❌ 权限系统
❌ UI页面（仅API）

---

# 📦 必须输出

1. 完整可运行代码
2. requirements.txt
3. 启动方式
4. curl 测试示例（文章 + 商品 + 发布）

---

# 💥 最终要求

代码必须：

✔ 可运行
✔ 可创建内容
✔ 可发布
✔ 可触发 deploy
✔ Task 正常工作

不允许：

❌ 空函数
❌ 假逻辑
❌ 仅结构无实现
二、给你一个关键优化提醒（很重要）

阶段二最容易翻车的地方只有 3 个：

⚠️ 1. publish 没走 Task Engine

错误写法：

publish_article()  # 直接执行

正确：

API → Task → PublishEngine → Deploy
⚠️ 2. 商品 price 不校验

你必须强制：

price:
✔ 必须 float
✔ > 0

否则 Bulk 阶段一定崩。

⚠️ 3. deploy 没触发

发布必须：

publish → deploy

否则系统就变成“写数据工具”，不是“网站工厂”。
你正在继续实现 Site Factory OS（SFS），当前是【阶段三：Bulk Import 系统】。

本阶段是系统核心模块，必须严格按照规范实现，不允许简化逻辑。

🎯 目标

实现：

📁 文件夹 → 多站内容 → 校验 → 批量导入 → 自动发布 → 失败报告

📦 必须实现模块

core/

f4_bulk_engine.py
bulk_validator.py
bulk_parser.py
bulk_executor.py
bulk_reporter.py

api/

bulk_api.py

storage/

bulk_reports.json
📁 支持输入结构（必须严格匹配）

bulk_upload/
site_001/
config.json
product.txt
article.txt
images/
1.jpg
2.jpg

🧠 执行流程（必须三阶段）
scan
validate（只检查，不执行）
execute（全部通过才执行）
❗关键规则（必须遵守）

❌ 禁止边执行边报错
✔ 必须 validate 全部通过才允许 execute

⚙️ 校验规则（必须实现）
config.json

必须字段：

site_id
template
site_type

否则：

BULK_CONFIG_MISSING

product.txt

必须规则：

name:
✔ 必填

price:
✔ 必填
✔ 必须 float
✔ > 0

否则：

BULK_PRODUCT_PRICE_INVALID

images:
✔ 每个文件必须存在于 images/

否则：

BULK_PRODUCT_IMAGE_NOT_FOUND

article.txt

title:
✔ 必填

content:
✔ 必填

否则：

BULK_ARTICLE_INVALID_FORMAT

images/

✔ 必须存在
否则：

BULK_IMAGE_FOLDER_MISSING

🚨 错误码（必须使用）

BULK_CONFIG_MISSING
BULK_CONFIG_INVALID
BULK_PRODUCT_INVALID_FORMAT
BULK_PRODUCT_PRICE_INVALID
BULK_PRODUCT_IMAGE_NOT_FOUND
BULK_ARTICLE_INVALID_FORMAT
BULK_IMAGE_FOLDER_MISSING
BULK_VALIDATION_FAILED
BULK_EXECUTION_FAILED

📄 错误返回结构（必须统一）

{
"error_code": "...",
"message": "...",
"file": "...",
"line": 1
}

📊 Report系统（必须实现）

每次 Bulk 必须生成 report：

{
"task_id": "...",
"total": 10,
"success": 8,
"failed": 2,
"errors": []
}

🔁 Retry机制（必须实现）

支持：

POST /bulk/retry/{task_id}

✔ 只重试失败项
✔ 不重新执行成功项

🌐 API（必须实现）

POST /bulk/upload
POST /bulk/validate
POST /bulk/execute
GET /bulk/report/{task_id}
POST /bulk/retry/{task_id}

⚠️ 强约束
Bulk 所有操作必须走 Task Engine
必须支持 request_id 幂等
同一 site_id 禁止并发任务
validate 阶段禁止写入 storage

execute 阶段必须触发：

Publish → Deploy

🔁 执行流程（必须实现）

bulk_upload
↓
Task Engine
↓
BulkEngine.scan
↓
BulkValidator.validate
↓
if error → 返回 report（不执行）
↓
BulkExecutor.execute
↓
PublishEngine
↓
DeployEngine
↓
生成 report
↓
Task → success

🧪 验收标准（必须全部通过）

✔ 能识别多个 site 文件夹
✔ 能解析 config.json
✔ 能校验 product.txt
✔ 能校验 images 存在
✔ 错误能定位到文件
✔ validate 不写入数据
✔ execute 才写入
✔ 成功自动 deploy
✔ 失败生成 report
✔ retry 只执行失败项

🚫 禁止实现

❌ I18n 多语言（阶段四再做）
❌ SEO
❌ UI界面
❌ 自动修复错误
❌ 模糊校验（必须严格）

📦 必须输出
完整可运行代码
requirements.txt
示例 bulk_upload 文件夹
curl 测试示例
report 示例输出
💥 最终要求

代码必须：

✔ 可运行
✔ 校验严格
✔ 错误清晰
✔ 支持 retry
✔ Task 可追踪

不允许：

❌ 简化流程
❌ 跳过 validate
❌ 混乱错误结构
❌ 不生成 report
二、这一阶段最关键的3个“生死点”

我帮你提前指出，不然 agent 很容易写歪：

⚠️ 1. validate 和 execute 混在一起（最常见错误）

错误：

if error:
    print(error)
else:
    execute()

正确：

validate → 100%通过 → 才 execute
⚠️ 2. 没有 report（会直接废掉 Bulk 系统）

你必须能看到：

{
  "success": 8,
  "failed": 2,
  "errors": [...]
}

否则你无法运营。

⚠️ 3. retry 写错（很多AI会重跑全部）

正确逻辑：

retry = 只处理 failed 项

你正在实现 Site Factory OS（SFS）系统的【阶段四：稳定性与系统强化】。

本阶段不新增业务功能，只强化：

* Task Engine（幂等 + 状态机 + 锁）
* 回滚系统（Rollback）
* 错误系统（Error Engine）
* 审计系统（Audit）
* 可观测性（日志 + 状态）

⚠️ 本阶段是“系统可靠性工程”，必须严格执行规则。

---

# 🎯 目标

让系统具备：

✔ 不重复执行
✔ 不并发冲突
✔ 出错可恢复
✔ 可追踪所有操作
✔ 可回滚关键操作

---

# 📦 必须实现模块

core/

* f8_task_engine.py（强化版）
* rollback_engine.py
* error_engine.py（统一错误）
* audit_engine.py
* lock_manager.py（新增）

api/

* task_api.py（增强）
* deploy_api.py（rollback支持）

storage/

* audit_logs.json
* error_logs.json

---

# 🧠 一、Task Engine（必须重构）

---

## 🔁 幂等规则（必须）

每个任务必须包含：

{
"request_id": "string"
}

规则：

if request_id 已存在：
→ 返回已有 task
→ 禁止创建新任务

错误码：

TASK_DUPLICATE_REQUEST_ID

---

## 🔄 状态机（必须严格）

状态流：

pending → running → success
↘ failed → retrying → running

禁止：

❌ 跳状态
❌ success → running

---

## 🔒 任务锁（必须实现）

同一 site_id：

❌ 不允许并发任务

规则：

if site_id 有 running task:
return TASK_ALREADY_RUNNING

---

## 📊 Task日志（必须）

每个 task 必须记录：

{
"task_id": "...",
"logs": [
"start build",
"dns created",
"deploy success"
]
}

---

# 🔁 二、回滚系统（Rollback Engine）

---

## 🟢 可回滚

✔ GitHub deploy
✔ CMS 发布（文章 / 商品）

---

## 🟡 半回滚

⚠️ Bulk Import
→ 不删除成功项
→ 只允许 retry

---

## 🔴 不可回滚

❌ Cloudflare zone 创建
❌ NS 修改
❌ 域名绑定

---

## ⚙️ GitHub回滚实现

必须记录：

{
"deploy_id": "...",
"commit_id": "..."
}

回滚逻辑：

rollback → revert commit → redeploy

---

## API（必须实现）

POST /deploy/rollback/{deploy_id}

---

# 🚨 三、Error Engine（必须统一）

---

## 所有错误必须返回：

{
"error_code": "...",
"message": "...",
"severity": "INFO | WARNING | ERROR | CRITICAL",
"retryable": true/false,
"user_action_required": true/false
}

---

## 错误日志必须记录：

error_logs.json

---

## 必须使用 error_code_dictionary.md

禁止：

❌ 自定义 error_code
❌ 直接 raise Exception

---

# 📜 四、Audit Engine（审计系统）

---

## 所有操作必须记录：

{
"trace_id": "...",
"request_id": "...",
"action": "create_site",
"status": "success",
"timestamp": "..."
}

---

## 存储：

audit_logs.json

---

## 必须支持：

GET /system/audit

---

# 🔍 五、系统可观测性（Observability）

---

## 必须实现：

GET /system/status

返回：

{
"sites": 10,
"tasks_running": 2,
"tasks_failed": 1
}

---

## 必须实现：

GET /system/logs

---

# ⚠️ 强约束（必须执行）

1. 所有 API 操作必须产生 trace_id
2. 所有 task 必须可追踪
3. 所有错误必须进入 error_engine
4. 所有写操作必须记录 audit
5. rollback 必须走 deploy_engine
6. 不允许 silent failure（必须返回错误）

---

# 🧪 验收标准（必须全部通过）

✔ 相同 request_id 不重复执行
✔ 同站点不能并发任务
✔ 任务状态严格流转
✔ 所有任务有日志
✔ deploy 可回滚
✔ 错误统一结构
✔ audit 完整记录
✔ system/status 正常
✔ system/logs 可查询

---

# 🚫 禁止实现

❌ 新业务模块
❌ UI
❌ Bulk修改
❌ CMS扩展
❌ SEO / I18n

---

# 📦 必须输出

1. 完整可运行代码
2. rollback 示例
3. task 重试示例
4. error 示例
5. audit 示例

---

# 💥 最终要求

系统必须：

✔ 稳定
✔ 可恢复
✔ 可追踪
✔ 可回滚

不允许：

❌ 重复执行
❌ 状态混乱
❌ 无日志
❌ 错误不可追踪

二、这一阶段真正解决的是什么？

你前面三阶段解决的是：

能建站
能发内容
能批量导入

而阶段四解决的是：

💥 系统不会死

🚨 三、最关键的3个“稳定性红线”
⚠️ 1. 幂等失败 → 系统直接爆炸
同一个请求执行2次
→ 创建2个站
→ deploy冲突
→ DNS错乱

👉 必须靠 request_id 防住

⚠️ 2. 没有任务锁 → 并发灾难
bulk + publish 同时执行
→ 覆盖数据
→ deploy乱序

👉 必须锁 site_id

⚠️ 3. 没有回滚 → 一次错误全崩
deploy失败
→ 网站挂
→ 无法恢复

👉 必须 commit级回滚