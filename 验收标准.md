一、总验收原则（强制）
1. 所有功能必须可运行（禁止只写结构）
2. 所有写操作必须通过 Task Engine
3. 所有 API 必须带 request_id（写操作）
4. 所有任务必须有 trace_id
5. 所有错误必须使用 error_code
6. 所有数据必须落 SQLite（禁止 JSON 持久化）
7. 所有模块必须可独立测试
8. 所有阶段必须“可完整跑通链路”
9. 不允许跨阶段实现功能（越界直接判 FAIL）
10. 验收必须包含：API测试 + DB校验 + 日志校验 + 任务链路校验
🧱 二、统一验收维度（每个模块必须通过）

每个功能必须通过：

1. API 层验收
2. Task 层验收
3. 数据层验收（SQLite）
4. 错误处理验收
5. 幂等性验收
6. 并发/锁验收
7. 日志/审计验收
🧩 三、全局强制检查项
3.1 Task Engine 验收（核心）
✔ request_id 幂等（重复请求返回同一个 task_id）
✔ trace_id 存在且贯穿全链路
✔ 状态机正确：

pending → running → success
                 ↘ failed → retrying

✔ retry_count 正确递增
✔ max_retry 生效（超出后 TASK_RETRY_LIMIT_EXCEEDED）
✔ site_id 任务锁生效（不可并发）
✔ 所有任务必须写入 tasks 表
✔ 所有节点必须写入 task_logs 表

数据库验证：

SELECT * FROM tasks WHERE request_id = 'xxx';
SELECT * FROM task_logs WHERE task_id = 'xxx';
3.2 error_code 验收（必须严格）

必须符合：

✔ 所有错误返回 error_code
✔ error_code 必须存在于 error_code_dictionary.md
✔ 不允许只返回 message
✔ retryable 字段必须正确
✔ user_action_required 必须正确
✔ severity 必须存在

示例验证结构必须满足：


3.3 API 统一规范验收

必须符合：


✔ REST 风格（/api/v1/...）
✔ 禁止旧接口风格（/site/create）
✔ 所有 POST/PATCH 必须带 request_id
✔ 异步操作必须返回 task_id
✔ GET 不允许产生副作用
3.4 SQLite 验收（禁止 JSON 持久化）
✔ 所有核心数据存在 SQLite
✔ 表结构完整（sites / tasks / bulk_jobs 等）
✔ 外键关系正确
✔ 不允许 storage/*.json 作为主存储
🚀 四、阶段验收标准（核心）
🟢 阶段一验收（建站链路）

（对应工单）


🎯 必须完成
创建站点 → GitHub → Cloudflare → Deploy → 可访问
✅ 功能验收
✔ POST /api/v1/sites 成功
✔ 返回 task_id
✔ 生成 site_id
✔ 写入 sites 表
✔ 创建 deployment 记录
✔ 创建 domain 记录
✔ 返回 NS
✔ DNS check 可触发
✔ deploy 成功
✔ live_url 可访问（mock 也可）
🧪 强制测试
1. 幂等测试
重复调用 /sites（相同 request_id）

必须：

✔ 返回同一个 task_id
✔ 不重复创建 site
2. 并发测试
同一 site_id 同时 deploy

必须：

✔ TASK_ALREADY_RUNNING
✔ resource_locks 表存在记录
3. DNS 测试
未设置 NS → deploy

必须：

✔ DNS_NS_NOT_PROPAGATED
✔ retryable = true
✔ user_action_required = true
🟡 阶段二验收（CMS / 商品 / 支付）
🎯 必须完成
内容创建 → 发布 → 触发 deploy
✅ 验收
✔ article 可创建
✔ product 可创建
✔ price 必须为数字
✔ payment 可创建
✔ publish 必须走 Task Engine
✔ publish 自动触发 deploy
✔ 页面可访问
❌ 错误验证（必须失败）
price = "abc"

必须：

CMS_PRODUCT_PRICE_INVALID
🔁 状态验证
draft → published → archived

必须：

✔ 状态正确流转
✔ published_at 写入
🔵 阶段三验收（Bulk 系统）
🎯 必须完成
scan → validate → execute → deploy
✅ 核心验收
✔ scan 不写 DB
✔ validate 不写 DB
✔ execute 才写 DB
✔ 所有错误写 bulk_errors
✔ 支持 preview
✔ 支持 report
✔ 支持 retry
🧪 强制测试
1. 文件错误
price = abc

必须：

BULK_PRODUCT_PRICE_INVALID
2. 图片缺失
image 不存在

必须：

BULK_PRODUCT_IMAGE_NOT_FOUND
3. Retry 验收（核心）
第一次 execute：失败 10 条
第二次 retry

必须：

✔ 只重试 failed_items
✔ success_items 不重复执行
✔ bulk_items 唯一约束生效
✔ retry_count 正确递增
🧱 DB 验证
SELECT * FROM bulk_items WHERE status='failed';
SELECT * FROM bulk_errors;
🔴 阶段四验收（稳定性 / 回滚）
🎯 必须完成
系统不炸 + 可回滚 + 可恢复
✅ 验收
✔ retry API 可用
✔ cancel API 可用
✔ rollback API 可用
✔ deploy rollback 成功
✔ 任务状态完整
✔ 所有操作有 audit_logs
🔁 回滚规则验证
GitHub deploy → rollback

必须：

✔ previous_commit_id 存在
✔ deployment.status = rollback_success
❌ 禁止行为
Bulk 自动删除成功项
DNS 自动回滚
🧠 五、链路级验收（必须全通）
🔗 完整链路 1（建站）
API → Task → Build → DNS → Deploy → 成功
🔗 完整链路 2（发布）
CMS → Publish → Deploy → 页面更新
🔗 完整链路 3（Bulk）
Scan → Validate → Execute → Deploy
🔗 完整链路 4（错误）
错误 → error_code → retry → success
🧪 六、自动验收脚本必须具备
必须自动检测：
✔ request_id 幂等
✔ DB 是否写入
✔ error_code 是否合法
✔ retryable 是否正确
✔ Task 状态机是否合法
✔ Bulk 是否分阶段执行
✔ 是否存在越权写入（绕过 Task Engine）
✔ 是否存在 JSON 持久化
✔ 是否存在未使用 error_code
❌ 自动拒绝执行条件
✖ 未使用 Task Engine
✖ 未写入 SQLite
✖ 使用 message 判断逻辑
✖ 未实现幂等
✖ Bulk 未分阶段
✖ error_code 不在字典中
✖ 存在并发写冲突
✖ API 不符合统一规范
🧨 七、最终判定标准
✅ PASS（通过）
✔ 所有 API 可调用
✔ 所有流程可跑通
✔ 所有 DB 数据正确
✔ 所有错误可控
✔ 所有任务可追踪
✔ 所有 retry 可恢复
✔ 所有模块独立运行
❌ FAIL（不通过）
✖ 任一模块无法运行
✖ 任一流程断链
✖ 任一错误不可追踪
✖ 任一 retry 不可恢复
✖ 任一数据未落库
✖ 任一 API 不规范