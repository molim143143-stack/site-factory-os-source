---

# 📄 AGENT_EXECUTION_SPEC.md

# Site Factory OS（SFS）Agent 执行规范（终版）

---

# 🧠 0. 核心目标（必须理解）

你正在实现一个系统：

```text
Site Factory OS（SFS）

= 多语言网站工厂操作系统
= 自动建站 + 批量内容发布 + 多站运营 + 自动部署
```

系统必须具备：

```text
✔ 可扩展
✔ 可追踪
✔ 可回滚
✔ 可审计
✔ 不重复执行
✔ 不产生脏数据
```

---

# ⚠️ 1. 全局强制规则（绝对不能违反）

```text
1. 所有写操作必须带 request_id（幂等）
2. 所有任务必须带 trace_id
3. 所有业务必须走 Task Engine
4. 禁止 API 层直接写数据库
5. 禁止绕过 ValidateNode
6. 禁止绕过 RequestIdempotencyNode
7. 同一 site_id 禁止并发任务
8. 所有错误必须使用 error_code
9. 所有错误必须结构化返回
10. 所有关键操作必须记录 audit_log
11. 所有 deploy 必须生成快照（deployment snapshot）
12. 所有 rollback 必须基于快照
13. DNS 操作不允许自动回滚
14. Bulk 必须 scan → validate → execute
15. validate 阶段禁止写入数据库
16. retry 只能处理失败项
17. 不允许重跑成功数据
18. 所有模块必须可独立运行和测试
```

---

# 🏗️ 2. 系统架构（必须遵守）

```text
Web / PWA（主控制台）
        ↓
Telegram（快捷控制）
        ↓
FastAPI Gateway
        ↓
Task Engine（唯一执行入口）
        ↓
Workflow Router
        ↓
Core Engines
        ↓
Integrations
        ↓
GitHub + Cloudflare
```

---

# 🧱 3. 技术栈强制要求

```text
语言：Python 3.11+
框架：FastAPI
数据库：SQLite（禁止 JSON 存储业务数据）
ORM：SQLAlchemy / SQLModel
```

---

# 🗄️ 4. 数据存储规则

```text
✔ 所有数据必须进入 SQLite
✔ 禁止使用 JSON 文件作为主存储
✔ JSON 仅允许：
   - config
   - logs导出
   - debug数据
```

---

# 🔁 5. Task Engine 规范（核心）

## 5.1 幂等规则

```text
if request_id 已存在：
    返回已有 task
    禁止创建新任务
```

错误码：

```text
TASK_DUPLICATE_REQUEST_ID
```

---

## 5.2 状态机（必须严格）

```text
pending → running → success
                ↘ failed → retrying → running
```

禁止：

```text
❌ success → running
❌ 跳状态
```

---

## 5.3 资源锁（必须）

```text
同一 site_id：

❌ 不允许并发任务
```

错误码：

```text
TASK_ALREADY_RUNNING
```

---

## 5.4 Task日志（必须）

```json
{
  "task_id": "...",
  "logs": [
    "start build",
    "dns created",
    "deploy success"
  ]
}
```

---

# 🚨 6. 错误处理规范（必须执行）

## 6.1 错误返回结构

```json
{
  "error": {
    "error_code": "XXX_XXX_XXX",
    "message": "human readable",
    "severity": "ERROR",
    "retryable": true,
    "user_action_required": false,
    "details": {},
    "trace_id": "trace_xxx",
    "request_id": "req_xxx"
  }
}
```

---

## 6.2 错误原则

```text
✔ 必须使用 error_code
✔ message 只用于人类
✔ 前端必须依赖 error_code
✔ 禁止随意新增 error_code
```

---

# 🚀 7. Deploy 快照规则（必须实现）

## 每次 deploy 必须记录：

```text
deploy_id
site_id
task_id
trace_id
request_id
commit_id
previous_commit_id
repo
branch
manifest_hash
dist_path
live_url
created_at
```

---

## 文件级快照

```text
每个文件必须记录：

file_path
content_hash
action（add/modify/delete）
```

---

## 回滚规则

```text
✔ 必须基于 deploy_id
✔ 使用 previous_commit_id
✔ rollback 也是一次 deploy
✔ DNS 不回滚
```

---

# 📦 8. Bulk Import 规范（核心模块）

## 8.1 三阶段

```text
scan
validate（禁止写入）
execute（才允许写入）
```

---

## 8.2 校验规则

### config.json

```text
site_id 必填
template 必填
site_type 必填
```

---

### product

```text
name 必填
price 必须 float
price > 0
images 必须存在
```

---

### article

```text
title 必填
content 必填
```

---

## 8.3 错误结构

```json
{
  "error_code": "BULK_PRODUCT_PRICE_INVALID",
  "file": "xxx",
  "line": 1,
  "field": "price"
}
```

---

## 8.4 Bulk Item 唯一标识（必须）

```text
source_hash = sha256(file + line + content)
```

---

## 8.5 Retry 规则（精确）

```text
只允许状态：

validation_failed
execute_failed
retry_failed
```

---

## Retry 流程

```text
重新读取源文件
重新 parse
重新 validate
validate 通过才 execute
```

---

## 禁止行为

```text
❌ 重跑成功数据
❌ 跳过 validate
❌ 修改历史成功记录
```

---

## Deploy 触发规则

```text
if 有成功变更：
    deploy
else：
    不 deploy
```

---

# 🌍 9. I18n 规则

```text
默认语言必须 en
所有语言 fallback 到 en
必须支持语言完整度检测
发布前必须检查语言完整度
```

---

# 🎨 10. SEO 规则

```text
每个语言独立：

title
description
slug

必须生成：

hreflang
sitemap.xml
```

---

# 🧩 11. DIY Builder 规则

```text
页面必须存储为 page.json
必须支持：
拖拽
排序
多语言字段
预览
发布
```

---

# 🔐 12. 权限与安全

## 高风险操作（必须确认）

```text
删除站点
修改 DNS
发布生产环境
回滚
批量导入
```

---

## 规则

```text
必须进入 waiting_confirm 状态
必须人工确认
```

---

# 📊 13. Agent 行为记录（必须输出）

每次执行必须记录：

```markdown
## 🧾 Execution Log

### Step: XXX
- Action:
- Input:
- Output:
- DB Changes:
- Files Changed:
- Error:
- Result:

### Step: XXX
...
```

---

# 📦 14. 代码修改规范

每次修改必须输出：

```markdown
## 🔧 Code Change

### 文件：
xxx.py

### 修改前：
（原代码片段）

### 修改后：
（完整可替换代码）

### 修改原因：
```

---

# 🧪 15. 验收规则

每个功能必须提供：

```text
✔ curl 示例
✔ 输入数据
✔ 返回数据
✔ 数据库变化
✔ 日志输出
```

---

# 🚫 16. 禁止行为（非常重要）

```text
❌ 写空函数
❌ 写伪代码
❌ 跳过 Task Engine
❌ API 直接写数据库
❌ 不记录日志
❌ 不使用 error_code
❌ 混用数据结构
❌ 重复实现模块
❌ 破坏已有结构
```

---

# 🧠 17. Agent 必须理解的核心思想

```text
你不是在写功能
你是在构建一个“可持续运行的系统”

优先级：

正确性 > 可追踪 > 可恢复 > 可扩展 > 功能数量
```

---

# 💥 18. 最终目标（不可偏离）

```text
构建一个：

✔ 多站管理
✔ 多语言发布
✔ 批量导入
✔ 自动部署
✔ 可回滚
✔ 可审计
✔ Telegram可控

的完整网站工厂系统
```

---

